# ESP32 Board Configuration

# The evaluation board uses ESP32 revision 1.0 silicon. Zephyr marks this
# revision unsupported, so builds for that board must opt in explicitly.
CONFIG_ESP32_USE_UNSUPPORTED_REVISION=y
